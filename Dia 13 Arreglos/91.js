// Recorriendo arreglos

const recorrido1 = ['juan', 15, 'jose', 25.6, true, false];
//para saber la longitud del arreglo
console.log(recorrido1.length); 
console.log(recorrido1);
console.table(recorrido1);

for (let i=0; i < recorrido1.length; i++) {
    console.log(recorrido1[i]);
}

